# Script di avvio per il Server Backend (Flask API)
# Uso: python run_server.py

import os
import sys

# Aggiunge la cartella corrente al path per gli import
sys.path.append(os.path.abspath(os.path.dirname(__file__)))

if __name__ == "__main__":
    print("Avvio del server di Gestione Ristorante in corso...")
    # Qui andrà l'import dell'applicazione Flask una volta implementata
    # from server.app import app
    # app.run(debug=True, port=5000)
    print("Nota: Il codice del server non è ancora stato implementato. Fai riferimento al piano di implementazione.")
